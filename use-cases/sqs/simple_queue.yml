---
# Blog applications

version: '3.8'

x-sqs:
  queueA:
    Settings:
      EnvNames:
        - QUEUEA

  queueB:
    Properties:
      RedrivePolicy:
        deadLetterTargetArn: queueA
        maxReceiveCount: 10
    Settings:
      EnvNames:
        - queueb
    Services:
      - name: bignicefamily
        access: RWMessages

  queueC:
    Properties:
      FifoQueue: True
    Services:
      - name: rproxy
        access: RWMessages
    Services:
      - name: youtoo
        access: RWMessages
    Settings:
      EnvNames:
        - FIFO_QUEUE
