services:
  app01:
    x-logging:
      CreateLogGroup: false
      RetentionInDays: 42
  app02:
    x-logging:
      RetentionInDays: 42
  app03:
    x-logging:
      CreateLogGroup: true
      RetentionInDays: 42
  rproxy:
    x-logging:
      CreateLogGroup: true
