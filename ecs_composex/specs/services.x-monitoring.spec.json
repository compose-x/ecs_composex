{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "id": "services.x-monitoring",
  "$id": "services.x-monitoring.spec.json",
  "type": "object",
  "title": "services.x-monitoring specification",
  "description": "The services.x-monitoring specification for ECS Compose-X",
  "additionalProperties": false,
  "properties": {
    "CWAgentCollectEmf": {
      "oneOf": [
        {
          "type": "boolean"
        },
        {
          "$ref": "#/definitions/EmfSidecarConfiguration"
        }
      ]
    },
    "CollectEmf": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "CloudwatchAgent": {
          "description": "Uses AWS CloudWatch agent sidecar for EMF ingestion",
          "$ref": "#/definitions/EmfSidecarConfiguration"
        },
        "FluentBit": {
          "description": "Uses FluentBit EMF ingestion. Not yet implemented.",
          "$ref": "#/definitions/EmfSidecarConfiguration"
        },
        "OTEL": {
          "description": "Uses ADOT Collector for EMF ingestion. Not yet implemented",
          "$ref": "#/definitions/EmfSidecarConfiguration"
        }
      }
    }
  },
  "definitions": {
    "EmfSidecarConfiguration": {
      "type": "object",
      "additionalProperties": false,
      "oneOf": [
        {
          "required": [
            "UseLatest"
          ]
        },
        {
          "required": [
            "OverrideImage"
          ]
        }
      ],
      "properties": {
        "UseLatest": {
          "type": "boolean",
          "description": "Use the latest image in the task definition"
        },
        "InterpolateWithDigest": {
          "type": "boolean",
          "description": "If true, will attempt to resolve the image Digest for the image and use in Task Definition"
        },
        "OverrideImage": {
          "type": "string",
          "description": "AT YOUR OWN RISKS - Allows to override the image to use."
        }
      }
    }
  }
}
