---
# Blog applications

version: '3.8'

x-sqs:
  queueA:
    Settings:
      EnvNames:
        - QUEUEA

  queueB:
    Properties:
      RedrivePolicy:
        deadLetterTargetArn: queueA
        maxReceiveCount: 10
    Settings:
      EnvNames:
        - queueb
    Services:
      - name: app01
        access: RWMessages
